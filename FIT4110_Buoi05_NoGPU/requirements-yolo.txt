# YOLO service dùng CPU thuần, không cần torch CUDA.
# ultralytics kéo theo torch CPU; numpy + pillow + pybase64 cho xử lý ảnh.
# Dùng PyTorch CPU-only wheel: https://download.pytorch.org/whl/cpu
# → tránh tải torch CUDA nặng 779 MB qua pip.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.27.2
ultralytics==8.3.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu
numpy==1.26.4
pillow==10.4.0
mysql-connector-python>=8.3,<9